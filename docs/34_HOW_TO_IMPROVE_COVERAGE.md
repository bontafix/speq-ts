# Как улучшить покрытие справочника параметров

## 📋 Обзор

После запуска `seed-parameter-dictionary-complete.ts` скрипт показывает непокрытые параметры. Есть **ТРИ способа** улучшить покрытие:

### 🚀 Способ 1: Автоматический через LLM (РЕКОМЕНДУЕТСЯ)

Используйте `generate-dictionary.ts` для автоматической генерации параметров через LLM:

1. **Проанализировать параметры:**
   ```bash
   npx tsx src/scripts/analyze-parameters.ts
   ```
   Создаст файл `parameter-analysis.json` со всеми параметрами из БД.

2. **Сгенерировать справочник через LLM:**
   ```bash
   npx tsx src/scripts/generate-dictionary.ts
   ```
   Автоматически создаст параметры для топ-60 наиболее частых параметров.

3. **Проверить результат:**
   ```bash
   npx tsx src/scripts/seed-parameter-dictionary-complete.ts
   ```
   Покажет, какие параметры остались непокрытыми.

**Преимущества:**
- ✅ Автоматически генерирует параметры через LLM
- ✅ Не требует ручного редактирования кода
- ✅ Обрабатывает все параметры из БД

**Ограничения:**
- ⚠️ Обрабатывает только топ-N параметров (по умолчанию 60, можно изменить через `MAX_PARAMS_TO_GENERATE`)
- ⚠️ Требует доступный LLM провайдер
- ⚠️ Может потребоваться ручная корректировка результатов

**Настройка:**
```bash
# Обработать больше параметров (например, 100)
MAX_PARAMS_TO_GENERATE=100 npx tsx src/scripts/generate-dictionary.ts

# Изменить минимальную частоту использования (по умолчанию 2)
MIN_PARAM_FREQUENCY=5 npx tsx src/scripts/generate-dictionary.ts
```

**Как это работает:**
1. `analyze-parameters.ts` анализирует ВСЕ параметры из `equipment.main_parameters`
2. Сохраняет результаты в `parameter-analysis.json` (все 204 параметра)
3. `generate-dictionary.ts` читает этот файл и для каждого параметра:
   - Отправляет запрос в LLM с примером значений
   - LLM генерирует структуру параметра (key, label_ru, aliases, priority и т.д.)
   - Сохраняет в БД напрямую

**Важно:** `generate-dictionary.ts` сохраняет параметры **напрямую в БД**, а не в код. Если нужно добавить их в `seed-parameter-dictionary-complete.ts`, нужно будет экспортировать из БД или скопировать вручную.

### ✏️ Способ 2: Ручное редактирование (для точного контроля)

Если нужен полный контроль или LLM недоступен:

1. **Добавить алиасы** к существующим параметрам (если параметр уже есть, но под другим названием)
2. **Создать новые параметры** (если параметра нет в справочнике)

---

## 1️⃣ Добавление алиасов к существующим параметрам

### Когда использовать:
- Параметр уже есть в справочнике, но в БД он называется по-другому
- Например: в справочнике есть `boom_length_m` ("Длина стрелы"), а в БД встречается "Длина выдвинутой стрелы, м."

### Как это сделать:

#### Шаг 1: Найдите параметр в массиве `parameters`

Откройте файл `src/scripts/seed-parameter-dictionary-complete.ts` и найдите нужный параметр. Например, для длины стрелы:

```typescript
{
  key: "boom_length_m",
  label_ru: "Длина стрелы",
  description_ru: "Длина стрелы в метрах",
  category: "crane",
  param_type: "number",
  unit: "m",
  min_value: 5,
  max_value: 100,
  aliases: [
    // Русские варианты
    "длина_стрелы",
    "длина стрелы",
    "стрела",
    "длина_стрелы_max",
    // Английские варианты
    "boom_length",
    "boom",
    "jib_length",
    "arm_length",
    // Из БД
    "Длина стрелы",
    "Макс. длина стрелы",
  ],
  sql_expression: "main_parameters->>'Длина стрелы'",
  priority: 15,
},
```

#### Шаг 2: Добавьте новый алиас в массив `aliases`

Добавьте название из БД в массив `aliases`. Например, если в отчете показано непокрытое "Длина выдвинутой стрелы, м.":

```typescript
aliases: [
  // Русские варианты
  "длина_стрелы",
  "длина стрелы",
  "стрела",
  "длина_стрелы_max",
  // Английские варианты
  "boom_length",
  "boom",
  "jib_length",
  "arm_length",
  // Из БД
  "Длина стрелы",
  "Макс. длина стрелы",
  "Длина выдвинутой стрелы, м.",  // ← ДОБАВЛЕНО
],
```

#### Пример из реального отчета:

**Непокрытый параметр:** "Длина выдвинутой стрелы, м." (24 раза)

**Решение:** Добавить в `boom_length_m`:

```typescript
{
  key: "boom_length_m",
  // ... остальные поля
  aliases: [
    // ... существующие алиасы
    "Длина выдвинутой стрелы, м.",  // ← Новый алиас
  ],
}
```

---

## 2️⃣ Создание новых параметров

### Когда использовать:
- Параметра нет в справочнике вообще
- Например: "Проектная длина", "Макс. подъемный момент"

### Как это сделать:

#### Шаг 1: Определите тип параметра

Посмотрите на примеры значений из отчета:
- **Числа** (35, 45, 48) → `param_type: "number"`
- **Текст с вариантами** ("Свободный", "Стационарный") → `param_type: "enum"`
- **Любой текст** → `param_type: "string"`
- **true/false** → `param_type: "boolean"`

#### Шаг 2: Создайте объект параметра

Найдите подходящее место в массиве `parameters` (по категории) и добавьте новый объект.

**Пример 1: Числовой параметр "Проектная длина"**

```typescript
// ========================================================================
// КРАНЫ - Проектная длина
// ========================================================================
{
  key: "project_length_m",
  label_ru: "Проектная длина",
  description_ru: "Проектная длина в метрах",
  category: "crane",
  param_type: "number",
  unit: "m",
  min_value: 50,
  max_value: 300,
  aliases: [
    // Из БД
    "Проектная длина",
    "проектная длина",
    "проектная_длина",
    // Английские варианты
    "project_length",
    "design_length",
  ],
  sql_expression: "main_parameters->>'Проектная длина'",  // Будет перезаписано автоматически
  priority: 20,  // Основная характеристика
},
```

**Пример 2: Числовой параметр "Макс. подъемный момент"**

```typescript
// ========================================================================
// КРАНЫ - Подъемный момент
// ========================================================================
{
  key: "lifting_moment_tm",
  label_ru: "Подъемный момент",
  description_ru: "Максимальный подъемный момент в тонно-метрах",
  category: "crane",
  param_type: "number",
  unit: "tm",  // тонно-метры
  min_value: 50,
  max_value: 500,
  aliases: [
    // Из БД
    "Макс. подъемный момент",
    "макс. подъемный момент",
    "макс подъемный момент",
    "максимальный подъемный момент",
    "подъемный момент",
    // Английские варианты
    "lifting_moment",
    "max_lifting_moment",
    "moment",
  ],
  sql_expression: "main_parameters->>'Макс. подъемный момент'",  // Будет перезаписано автоматически
  priority: 15,  // Основная характеристика
},
```

**Пример 3: Enum параметр "Вариант установки"**

```typescript
// ========================================================================
// ОБЩИЕ - Вариант установки
// ========================================================================
{
  key: "installation_type",
  label_ru: "Вариант установки",
  description_ru: "Тип установки оборудования",
  category: "common",
  param_type: "enum",
  enum_values: {
    free: "Свободный",
    stationary: "Стационарный",
    stationary_tower: "Стационарный башенный",
  },
  aliases: [
    // Из БД
    "Вариант установки",
    "вариант установки",
    "вариант_установки",
    "установка",
    // Английские варианты
    "installation_type",
    "installation",
    "mounting_type",
  ],
  sql_expression: "main_parameters->>'Вариант установки'",  // Будет перезаписано автоматически
  priority: 30,  // Второстепенная характеристика
},
```

#### Шаг 3: Определите приоритет

Приоритет определяет, где параметр будет отображаться в UI:

- **0-9**: Критически важные (Бренд, Модель, Год, Цена) - всегда видны
- **10-29**: Основные характеристики (Вес, Мощность, Грузоподъемность) - видны в фильтрах
- **30-49**: Второстепенные (Тип двигателя, Экологический класс) - в "Дополнительных фильтрах"
- **50-79**: Детали (Размеры, Объемы) - только в карточке товара
- **80-100**: Технические/мусор (Код запчасти) - скрыты

#### Шаг 4: Определите категорию

- `"excavator"` - для экскаваторов
- `"crane"` - для кранов
- `"truck"` - для грузовиков
- `"loader"` - для погрузчиков
- `"bulldozer"` - для бульдозеров
- `"common"` - для общих параметров

---

## 📝 Структура объекта ParameterEntry

```typescript
interface ParameterEntry {
  key: string;                    // Уникальный ключ (snake_case, латиница)
  label_ru: string;                // Название на русском
  description_ru: string;          // Описание
  category: string;                // Категория оборудования
  param_type: "number" | "enum" | "boolean" | "string";
  unit?: string;                   // Единица измерения (только для number)
  min_value?: number;             // Минимальное значение (только для number)
  max_value?: number;             // Максимальное значение (только для number)
  enum_values?: Record<string, string>;  // Значения enum (только для enum)
  aliases: string[];               // Массив алиасов (варианты названий)
  sql_expression: string;          // SQL выражение (автоматически генерируется)
  priority: number;                // Приоритет отображения (0-100)
}
```

---

## 🔍 Примеры из реального отчета

### Пример 1: "Длина выдвинутой стрелы, м." (24 раза)

**Решение:** Добавить алиас к существующему `boom_length_m`

```typescript
{
  key: "boom_length_m",
  // ...
  aliases: [
    // ... существующие
    "Длина выдвинутой стрелы, м.",  // ← Добавить
  ],
}
```

### Пример 2: "Проектная длина" (18 раз)

**Решение:** Создать новый параметр

```typescript
{
  key: "project_length_m",
  label_ru: "Проектная длина",
  description_ru: "Проектная длина в метрах",
  category: "crane",
  param_type: "number",
  unit: "m",
  min_value: 50,
  max_value: 300,
  aliases: [
    "Проектная длина",
    "проектная длина",
    "project_length",
  ],
  sql_expression: "main_parameters->>'Проектная длина'",
  priority: 20,
},
```

### Пример 3: "Макс. ширина укладки" (13 раз)

**Решение:** Создать новый параметр

```typescript
{
  key: "max_paving_width_m",
  label_ru: "Максимальная ширина укладки",
  description_ru: "Максимальная ширина укладки в метрах",
  category: "common",
  param_type: "number",
  unit: "m",
  min_value: 1,
  max_value: 20,
  aliases: [
    "Макс. ширина укладки",
    "макс. ширина укладки",
    "максимальная ширина укладки",
    "ширина укладки",
    "max_paving_width",
  ],
  sql_expression: "main_parameters->>'Макс. ширина укладки'",
  priority: 25,
},
```

---

## ✅ После внесения изменений

### Если использовали автоматический подход (LLM):

1. **Проверьте результат в БД:**
   ```sql
   SELECT * FROM parameter_dictionary ORDER BY priority, key;
   ```

2. **Запустите проверку покрытия:**
   ```bash
   npx tsx src/scripts/seed-parameter-dictionary-complete.ts
   ```

3. **При необходимости скорректируйте параметры** в БД или добавьте недостающие вручную

4. **Запустите нормализацию:**
   ```bash
   npx tsx src/scripts/normalize-parameters.ts
   ```

### Если использовали ручное редактирование:

1. **Сохраните файл** `src/scripts/seed-parameter-dictionary-complete.ts`

2. **Запустите скрипт снова:**
   ```bash
   npx tsx src/scripts/seed-parameter-dictionary-complete.ts
   ```

3. **Проверьте отчет** - непокрытые параметры должны уменьшиться

4. **Запустите нормализацию:**
   ```bash
   npx tsx src/scripts/normalize-parameters.ts
   ```

---

## 💡 Советы

1. **Всегда добавляйте точное название из БД** в алиасы (с учетом регистра и знаков препинания)

2. **Добавляйте варианты написания:**
   - С пробелами и без
   - С точками и без
   - С заглавными и строчными буквами
   - Русские и английские варианты

3. **Группируйте параметры по категориям** - это упрощает навигацию в коде

4. **Используйте осмысленные ключи** в формате `snake_case`:
   - ✅ `boom_length_m` 
   - ❌ `param1`, `length`

5. **Проверяйте примеры значений** из отчета - они помогут определить тип параметра

---

## 🎯 Цель

Довести покрытие до **100%** или хотя бы до **95%+** для технических параметров. Метаданные (производитель, модель, артикул) можно игнорировать.
