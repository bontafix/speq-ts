# Подробное руководство по нормализации параметров (30.12.2025)

## 📋 Что такое нормализация параметров?

**Нормализация** - это процесс преобразования сырых параметров из `main_parameters` в стандартизированный (canonical) формат в `normalized_parameters`.

### Пример преобразования:

```json
// ДО: main_parameters (сырые данные из разных источников)
{
  "Мощность двигателя": "132 л.с.",           // Разные единицы
  "Рабочий вес": "13500 кг",                  // Разные названия
  "Тип топлива": "Дизельный",                 // Текстовые значения
  "Глубина копания макс": "5 метров",         // Разный формат
  "Макс. глубина копания, мм.": "5000"        // Дубликаты
}

// ПОСЛЕ: normalized_parameters (canonical формат)
{
  "engine_power_kw": 97.152,                  // Единая единица (кВт)
  "operating_weight_t": 13.5,                 // Единая единица (тонны)
  "fuel_type": "diesel",                      // Enum значение
  "excavation_depth_mm": 5000                 // Единая единица (мм)
}
```

### Что происходит при нормализации:

1. **Маппинг названий** - "Мощность двигателя" → "engine_power_kw"
2. **Конверсия единиц** - "132 л.с." → 97.152 кВт
3. **Нормализация enum** - "Дизельный" → "diesel"
4. **Удаление дубликатов** - оставляется одно значение
5. **Валидация** - проверка диапазонов (min/max)

---

## 🎯 Зачем нужна нормализация?

### Проблемы без нормализации:

```sql
-- ❌ Плохо: разные названия, разные единицы
SELECT * FROM equipment
WHERE main_parameters->>'Мощность двигателя' IS NOT NULL
   OR main_parameters->>'Мощность' IS NOT NULL
   OR main_parameters->>'Номин. мощность, кВт.' IS NOT NULL;

-- ❌ Плохо: нужно конвертировать на лету
SELECT * FROM equipment
WHERE (main_parameters->>'Мощность двигателя')::text LIKE '%л.с.%';
```

### С нормализацией:

```sql
-- ✅ Хорошо: одно название, одна единица
SELECT * FROM equipment
WHERE (normalized_parameters->>'engine_power_kw')::numeric >= 100;

-- ✅ С индексом - очень быстро!
```

---

## 🚀 Как запустить нормализацию

### Шаг 1: Подготовка - заполнить справочник

**Справочник параметров** (`parameter_dictionary`) - это база знаний о параметрах:
- Какие есть параметры (engine_power_kw, operating_weight_t...)
- Какие у них алиасы ("мощность", "power", "Мощность двигателя"...)
- Какие единицы измерения (кВт, тонны, мм...)
- Как конвертировать (л.с. → кВт, тонны → кг...)

```bash
# 1. Заполнить справочник (если еще не сделано)
npx tsx src/scripts/seed-parameter-dictionary-complete.ts
```

**Что произойдет:**
```
🌱 Заполнение ПОЛНОГО справочника параметров...
================================================================================
📝 Миграция на 100% словарный подход
================================================================================

✅ Таблица parameter_dictionary готова

🗑️  Удалено старых записей: 10

✅ excavation_depth_mm         | Глубина копания            | 20 алиасов
✅ bucket_capacity_m3          | Объем ковша                | 15 алиасов
✅ operating_weight_t          | Рабочий вес                | 20 алиасов
✅ lifting_capacity_t          | Грузоподъемность           | 15 алиасов
✅ lifting_height_m            | Высота подъема             | 18 алиасов
✅ boom_reach_m                | Вылет стрелы               | 15 алиасов
✅ boom_length_m               | Длина стрелы               | 12 алиасов
✅ engine_power_kw             | Мощность двигателя         | 20 алиасов
✅ fuel_type                   | Тип топлива                | 12 алиасов
✅ drive_type                  | Тип привода                | 15 алиасов
✅ blade_capacity_m3           | Объем отвала               | 12 алиасов
✅ loader_bucket_capacity_m3  | Вместимость ковша погрузчика | 10 алиасов
✅ capacity_tph                | Производительность         | 12 алиасов
✅ year                        | Год выпуска                | 10 алиасов
✅ travel_speed_kmh            | Скорость передвижения      | 10 алиасов
✅ fuel_tank_capacity_l        | Объем топливного бака      | 8 алиасов

📊 Результаты заполнения:
   ✅ Добавлено параметров: 16
   ❌ Ошибок: 0
   📦 Всего параметров: 16
   🏷️  Всего алиасов: 204
   📈 Среднее алиасов на параметр: 12

📈 Статистика по категориям:
   - common        : 5 параметров, 72 алиасов
   - excavator     : 3 параметров, 50 алиасов
   - crane         : 3 параметров, 45 алиасов
   - bulldozer     : 1 параметров, 12 алиасов
   - loader        : 1 параметров, 10 алиасов

🏆 Топ-5 параметров по количеству алиасов:
   1. Рабочий вес (operating_weight_t): 20 алиасов
   2. Глубина копания (excavation_depth_mm): 20 алиасов
   3. Мощность двигателя (engine_power_kw): 20 алиасов
   4. Высота подъема (lifting_height_m): 18 алиасов
   5. Грузоподъемность (lifting_capacity_t): 15 алиасов

✨ Справочник успешно заполнен!
🎯 Система готова к работе в 100% словарном режиме
```

---

### Шаг 2: Запуск нормализации

```bash
# Полная нормализация всех записей
npx tsx src/scripts/normalize-parameters.ts

# Или пакетами (для больших БД)
NORMALIZE_BATCH_SIZE=100 npx tsx src/scripts/normalize-parameters.ts
```

**Что произойдет:**

```
Нормализация параметров оборудования...

Загрузка справочника параметров...
Загружено 16 параметров из справочника

Поиск записей для нормализации...
Найдено 150 записей для нормализации

Начало нормализации...

[1/150] Обработка: 123e4567-e89b-12d3-a456-426614174000...
  Исходные параметры:
    - "Мощность двигателя": "132 л.с."
    - "Рабочий вес": "13500 кг"
    - "Тип топлива": "Дизельный"
  
  Нормализованные параметры:
    - "engine_power_kw": 97.152 (из "132 л.с.")
    - "operating_weight_t": 13.5 (из "13500 кг")
    - "fuel_type": "diesel" (из "Дизельный")
  
  ✓ Нормализовано: 3, неразрешённых: 0, confidence: 100.0%

[2/150] Обработка: 223e4567-e89b-12d3-a456-426614174001...
  Исходные параметры:
    - "Глубина копания": "5 метров"
    - "Объем ковша": "1.5 м3"
    - "Неизвестный параметр": "какое-то значение"
  
  Нормализованные параметры:
    - "excavation_depth_mm": 5000 (из "5 метров")
    - "bucket_capacity_m3": 1.5 (из "1.5 м3")
  
  Неразрешённые параметры:
    - "Неизвестный параметр": "какое-то значение" (нет в справочнике)
  
  ✓ Нормализовано: 2, неразрешённых: 1, confidence: 66.7%

[3/150] Обработка: 323e4567-e89b-12d3-a456-426614174002...
...

================================================================================
РЕЗУЛЬТАТЫ
================================================================================
Обработано записей: 150
Успешно: 150
Ошибок: 0
Всего нормализовано параметров: 850
Всего неразрешённых параметров: 180
Средний confidence: 82.5%

✓ Все записи нормализованы!
```

---

## 🔍 Что такое "неразрешённые параметры"?

**Неразрешённые параметры (unresolved)** - это параметры из `main_parameters`, которые **не удалось** нормализовать.

### Причины:

#### 1. **Параметр не найден в справочнике**

```json
// main_parameters
{
  "Неизвестный параметр": "100",
  "Экзотическая характеристика": "значение"
}

// Справочник не знает эти параметры
// → Они попадают в unresolved
```

#### 2. **Не удалось распознать единицу измерения**

```json
// main_parameters
{
  "Мощность": "100 попугаев"  // Неизвестная единица
}

// UnitParser не может конвертировать
// → Попадает в unresolved
```

#### 3. **Не удалось распарсить значение**

```json
// main_parameters
{
  "Мощность": "очень большая",  // Не число
  "Вес": "н/д"                  // Нет данных
}

// Невозможно извлечь число
// → Попадают в unresolved
```

#### 4. **Значение вне диапазона**

```json
// main_parameters
{
  "Мощность": "10000 кВт"  // Слишком большое
}

// Справочник: max_value = 2000
// → Может быть отброшено как ошибка
```

---

## 🔧 Как исправить неразрешённые параметры

### Решение 1: Добавить алиасы в справочник

**Проблема:**
```
Неразрешённый параметр: "Номинальная мощность двигателя"
```

**Решение:** Добавить алиас в справочник

```typescript
// src/scripts/seed-parameter-dictionary-complete.ts
{
  key: "engine_power_kw",
  label_ru: "Мощность двигателя",
  aliases: [
    "мощность",
    "мощность_двигателя",
    "номинальная_мощность",
    // ✅ Добавить новый алиас:
    "Номинальная мощность двигателя",  // <-- НОВЫЙ
  ],
  // ...
}
```

Затем:
```bash
# Перезаполнить справочник
npx tsx src/scripts/seed-parameter-dictionary-complete.ts

# Повторить нормализацию
npx tsx src/scripts/normalize-parameters.ts
```

---

### Решение 2: Добавить новый параметр в справочник

**Проблема:**
```
Неразрешённый параметр: "Скорость вращения шнека"
```

**Решение:** Создать новый параметр

```typescript
// src/scripts/seed-parameter-dictionary-complete.ts

// Добавить в массив parameters:
{
  key: "auger_rotation_speed_rpm",
  label_ru: "Скорость вращения шнека",
  description_ru: "Скорость вращения шнека в оборотах в минуту",
  category: "common",
  param_type: "number",
  unit: "rpm",
  min_value: 0,
  max_value: 10000,
  aliases: [
    "скорость_вращения_шнека",
    "скорость шнека",
    "обороты_шнека",
    "rpm_шнека",
    "auger_speed",
    "auger_rotation",
  ],
  sql_expression: "normalized_parameters->>'auger_rotation_speed_rpm'",
  priority: 2,
}
```

Затем:
```bash
# Перезаполнить справочник
npx tsx src/scripts/seed-parameter-dictionary-complete.ts

# Повторить нормализацию
npx tsx src/scripts/normalize-parameters.ts
```

---

### Решение 3: Улучшить UnitParser

**Проблема:**
```
Неразрешённый параметр: "Мощность": "180 hp"
```

**Решение:** Добавить поддержку единицы "hp" в UnitParser

```typescript
// src/normalization/unit-parser.ts

private detectUnit(str: string): string {
  const lower = str.toLowerCase();
  
  // Мощность
  if (lower.includes('л.с.') || lower.includes('hp') || lower.includes('лс')) return 'hp';
  if (lower.includes('hp')) return 'hp';  // ✅ Добавить "hp"
  // ...
}
```

---

### Решение 4: Исправить данные в источнике

**Проблема:**
```
Неразрешённый параметр: "Мощность": "н/д"
```

**Решение:** Исправить в источнике данных или удалить

```sql
-- Найти проблемные записи
SELECT 
  id,
  name,
  main_parameters->>'Мощность' as power
FROM equipment
WHERE main_parameters->>'Мощность' = 'н/д';

-- Удалить некорректный параметр
UPDATE equipment
SET main_parameters = main_parameters - 'Мощность'
WHERE main_parameters->>'Мощность' = 'н/д';
```

---

## 📊 Анализ неразрешённых параметров

### Скрипт для анализа:

```typescript
// src/scripts/analyze-unresolved.ts

import { pgPool } from "../db/pg";

async function analyzeUnresolved() {
  const sql = `
    SELECT 
      id::text,
      name,
      jsonb_object_keys(main_parameters) as param_key
    FROM equipment
    WHERE is_active = true
      AND normalized_parameters IS NOT NULL
      AND jsonb_array_length(
        jsonb_object_keys(main_parameters)
      ) > jsonb_array_length(
        jsonb_object_keys(normalized_parameters)
      )
  `;
  
  const result = await pgPool.query(sql);
  
  // Подсчитываем частоту неразрешённых параметров
  const frequency: Record<string, number> = {};
  
  for (const row of result.rows) {
    const key = row.param_key;
    frequency[key] = (frequency[key] || 0) + 1;
  }
  
  // Сортируем по частоте
  const sorted = Object.entries(frequency)
    .sort((a, b) => b[1] - a[1]);
  
  console.log("Топ-20 неразрешённых параметров:\n");
  
  sorted.slice(0, 20).forEach(([key, count], index) => {
    console.log(`${index + 1}. "${key}" - ${count} раз`);
  });
  
  await pgPool.end();
}

analyzeUnresolved();
```

Запуск:
```bash
npx tsx src/scripts/analyze-unresolved.ts
```

**Вывод:**
```
Топ-20 неразрешённых параметров:

1. "Производитель" - 150 раз
2. "Модель" - 150 раз
3. "Серийный номер" - 145 раз
4. "Дата производства" - 140 раз
5. "Цвет" - 120 раз
6. "Гарантия" - 100 раз
...
```

**Анализ:** Большинство неразрешённых - это метаданные, а не технические параметры. Их можно:
- Игнорировать (не критичны для поиска)
- Добавить в справочник (если важны)
- Хранить только в `main_parameters`

---

## 🔄 Обновление справочника

### Сценарий 1: Добавление новых параметров

```bash
# 1. Отредактировать seed-parameter-dictionary-complete.ts
#    Добавить новые параметры в массив

# 2. Перезаполнить справочник
npx tsx src/scripts/seed-parameter-dictionary-complete.ts

# 3. Пересчитать нормализацию (опционально)
#    Только для записей с новыми параметрами
psql -d equipment_catalog -c "
  UPDATE equipment 
  SET normalized_parameters = '{}'::jsonb 
  WHERE main_parameters ? 'новый_параметр'
"

# 4. Запустить нормализацию
npx tsx src/scripts/normalize-parameters.ts
```

---

### Сценарий 2: Добавление алиасов

```bash
# 1. Добавить алиасы в seed-parameter-dictionary-complete.ts

# 2. Перезаполнить справочник
npx tsx src/scripts/seed-parameter-dictionary-complete.ts

# 3. Пересчитать для записей с этими алиасами
psql -d equipment_catalog -c "
  UPDATE equipment 
  SET normalized_parameters = '{}'::jsonb 
  WHERE jsonb_object_keys(main_parameters) && ARRAY['новый_алиас']
"

# 4. Запустить нормализацию
npx tsx src/scripts/normalize-parameters.ts
```

---

### Сценарий 3: Изменение логики конверсии

```bash
# 1. Изменить UnitParser или EnumMapper

# 2. Пересчитать ВСЕ записи
psql -d equipment_catalog -c "
  UPDATE equipment 
  SET normalized_parameters = '{}'::jsonb
"

# 3. Запустить полную нормализацию
npx tsx src/scripts/normalize-parameters.ts
```

---

## 📈 Мониторинг качества нормализации

### SQL запросы для проверки:

```sql
-- 1. Процент нормализованных записей
SELECT 
  COUNT(*) as total,
  COUNT(normalized_parameters) FILTER (WHERE normalized_parameters != '{}'::jsonb) as normalized,
  ROUND(
    100.0 * COUNT(normalized_parameters) FILTER (WHERE normalized_parameters != '{}'::jsonb) / COUNT(*),
    2
  ) as percent_normalized
FROM equipment
WHERE is_active = true;

-- 2. Средний confidence
SELECT 
  AVG(
    jsonb_array_length(jsonb_object_keys(normalized_parameters))::float / 
    NULLIF(jsonb_array_length(jsonb_object_keys(main_parameters)), 0)
  ) * 100 as avg_confidence
FROM equipment
WHERE is_active = true
  AND normalized_parameters != '{}'::jsonb
  AND main_parameters != '{}'::jsonb;

-- 3. Записи с низким confidence
SELECT 
  id::text,
  name,
  jsonb_array_length(jsonb_object_keys(main_parameters)) as total_params,
  jsonb_array_length(jsonb_object_keys(normalized_parameters)) as normalized_params,
  ROUND(
    100.0 * jsonb_array_length(jsonb_object_keys(normalized_parameters)) / 
    NULLIF(jsonb_array_length(jsonb_object_keys(main_parameters)), 0),
    1
  ) as confidence
FROM equipment
WHERE is_active = true
  AND normalized_parameters != '{}'::jsonb
  AND jsonb_array_length(jsonb_object_keys(normalized_parameters))::float / 
      NULLIF(jsonb_array_length(jsonb_object_keys(main_parameters)), 0) < 0.5
ORDER BY confidence ASC
LIMIT 10;
```

---

## ✅ Чеклист нормализации

- [ ] Справочник заполнен (16+ параметров, 200+ алиасов)
- [ ] Запущена нормализация всех записей
- [ ] Проверен процент нормализованных (>80%)
- [ ] Проанализированы неразрешённые параметры
- [ ] Добавлены важные алиасы в справочник
- [ ] Создан новые параметры (если нужно)
- [ ] Повторная нормализация выполнена
- [ ] Индексы созданы (для производительности)
- [ ] Поиск протестирован

---

## 🎉 Итог

**Нормализация параметров** - это процесс преобразования сырых данных в стандартизированный формат для:
- ✅ Быстрого поиска (с индексами)
- ✅ Точных запросов (одинаковые единицы)
- ✅ Простоты разработки (canonical формат)

**Неразрешённые параметры** - это нормально! Их можно:
- Добавить в справочник (если важны)
- Игнорировать (если не критичны)
- Исправить в источнике (если ошибка)

**Обновление справочника** - простое:
- Добавить параметры/алиасы в seed скрипт
- Перезапустить seed
- Пересчитать нормализацию

**Готово к использованию! 🚀**

