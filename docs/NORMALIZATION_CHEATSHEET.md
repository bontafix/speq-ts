# 📝 Шпаргалка по нормализации параметров

## 🚀 Быстрый старт

```bash
# 1. Заполнить справочник параметров
npx tsx src/scripts/seed-parameter-dictionary-complete.ts

# 2. Нормализовать все записи
npx tsx src/scripts/normalize-parameters.ts

# 3. Проанализировать неразрешённые параметры
npx tsx src/scripts/analyze-unresolved-parameters.ts

# 4. Создать индексы (опционально, для скорости)
psql -d equipment_catalog -f migrations/009_create_normalized_indexes.sql
```

---

## 📚 Что такое нормализация?

**Преобразование сырых данных → стандартный формат:**

```
main_parameters                     normalized_parameters
{"Мощность": "132 л.с."}      →    {"engine_power_kw": 97.152}
{"Вес": "13500 кг"}           →    {"operating_weight_t": 13.5}
{"Топливо": "Дизель"}         →    {"fuel_type": "diesel"}
```

---

## ❓ Что такое неразрешённые параметры?

**Параметры, которые НЕ удалось нормализовать:**

### Причины:
1. **Нет в справочнике** - параметр не известен системе
2. **Неизвестная единица** - "100 попугаев"
3. **Не число** - "очень большая"
4. **Вне диапазона** - слишком большое/малое значение

### Это нормально!
- Метаданные (производитель, модель) - можно игнорировать
- Редкие параметры - добавить в справочник при необходимости

---

## 🔧 Как исправить неразрешённые параметры

### Вариант 1: Добавить алиас

```typescript
// src/scripts/seed-parameter-dictionary-complete.ts
{
  key: "engine_power_kw",
  aliases: [
    "мощность",
    "power",
    "Новый алиас здесь",  // <-- Добавить
  ],
}
```

### Вариант 2: Создать новый параметр

```typescript
// src/scripts/seed-parameter-dictionary-complete.ts
{
  key: "new_parameter",
  label_ru: "Новый параметр",
  category: "common",
  param_type: "number",
  unit: "unit",
  aliases: ["алиас1", "алиас2"],
  sql_expression: "normalized_parameters->>'new_parameter'",
  priority: 2,
}
```

### Применить изменения:

```bash
# 1. Перезаполнить справочник
npx tsx src/scripts/seed-parameter-dictionary-complete.ts

# 2. Пересчитать нормализацию
npx tsx src/scripts/normalize-parameters.ts
```

---

## 🔍 Полезные SQL запросы

### Проверить процент нормализации:
```sql
SELECT 
  COUNT(*) as total,
  COUNT(normalized_parameters) FILTER (WHERE normalized_parameters != '{}'::jsonb) as normalized,
  ROUND(100.0 * COUNT(normalized_parameters) FILTER (WHERE normalized_parameters != '{}'::jsonb) / COUNT(*), 2) as percent
FROM equipment
WHERE is_active = true;
```

### Найти записи с низким confidence:
```sql
SELECT 
  id::text,
  name,
  jsonb_object_keys(main_parameters) as main_count,
  jsonb_object_keys(normalized_parameters) as norm_count
FROM equipment
WHERE is_active = true
  AND jsonb_array_length(jsonb_object_keys(normalized_parameters)) < 
      jsonb_array_length(jsonb_object_keys(main_parameters)) / 2
LIMIT 10;
```

### Посмотреть примеры нормализации:
```sql
SELECT 
  name,
  main_parameters,
  normalized_parameters
FROM equipment
WHERE is_active = true
  AND normalized_parameters != '{}'::jsonb
LIMIT 5;
```

---

## 📊 Структура справочника

```typescript
interface ParameterEntry {
  key: string;                  // "engine_power_kw" - canonical ключ
  label_ru: string;             // "Мощность двигателя" - русское название
  description_ru: string;       // Описание
  category: string;             // "common", "excavator", "crane"...
  param_type: "number"|"enum"|"boolean";  // Тип параметра
  unit?: string;                // "kw", "t", "mm" - единица измерения
  min_value?: number;           // Минимум (для валидации)
  max_value?: number;           // Максимум (для валидации)
  enum_values?: Record<string, string>;  // Для enum типа
  aliases: string[];            // ["мощность", "power", ...] - алиасы
  sql_expression: string;       // SQL для доступа к значению
  priority: number;             // Приоритет при поиске
}
```

---

## 🎯 Workflow обновления справочника

```
┌─────────────────────────────────────┐
│ 1. Анализ неразрешённых параметров  │
│    npx tsx analyze-unresolved...    │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│ 2. Редактирование справочника       │
│    seed-parameter-dictionary...ts   │
│    - Добавить алиасы                │
│    - Добавить параметры             │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│ 3. Перезаполнение справочника       │
│    npx tsx seed-parameter...        │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│ 4. Пересчёт нормализации            │
│    npx tsx normalize-parameters.ts  │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│ 5. Проверка результата              │
│    npx tsx analyze-unresolved...    │
└─────────────────────────────────────┘
```

---

## 🔄 Автоматизация

### Вариант 1: Cron задача
```bash
# Нормализация новых записей каждый час
0 * * * * cd /path/to/speq-ts && npx tsx src/scripts/normalize-parameters.ts
```

### Вариант 2: Worker процесс
```typescript
// Непрерывная нормализация в фоне
setInterval(async () => {
  await normalizeNewRecords();
}, 60000); // каждую минуту
```

### Вариант 3: Trigger в БД
```sql
-- Сброс normalized_parameters при изменении main_parameters
CREATE TRIGGER equipment_reset_normalized
BEFORE UPDATE ON equipment
FOR EACH ROW
EXECUTE FUNCTION reset_normalized_on_update();
```

---

## 📈 Метрики качества

| Метрика | Цель | Как проверить |
|---------|------|---------------|
| **Процент нормализации** | >95% | SQL: COUNT(normalized != '{}') / COUNT(*) |
| **Средний confidence** | >80% | Среднее из norm_keys / main_keys |
| **Неразрешённых параметров** | <50 | analyze-unresolved-parameters.ts |
| **Технических параметров в справочнике** | >20 | COUNT(*) FROM parameter_dictionary |

---

## 🚨 Troubleshooting

### Проблема: "Справочник пуст"
```bash
# Заполнить справочник
npx tsx src/scripts/seed-parameter-dictionary-complete.ts
```

### Проблема: "Все параметры неразрешённые"
```bash
# 1. Проверить справочник
psql -d equipment_catalog -c "SELECT COUNT(*) FROM parameter_dictionary;"

# 2. Если пуст - заполнить
npx tsx src/scripts/seed-parameter-dictionary-complete.ts
```

### Проблема: "Низкий confidence"
```bash
# Анализ проблемных параметров
npx tsx src/scripts/analyze-unresolved-parameters.ts

# Добавить алиасы/параметры в справочник
# Повторить нормализацию
```

---

## 📚 Документация

- **Полное руководство:** `docs/27_NORMALIZATION_DETAILED_GUIDE.md`
- **Использование normalized_parameters:** `docs/26_NORMALIZED_PARAMETERS_USAGE.md`
- **План миграции:** `docs/25_MIGRATION_TO_DICTIONARY.md`
- **Краткое руководство:** `NORMALIZED_PARAMETERS_GUIDE.md`

---

## ✅ Чеклист

- [ ] Справочник заполнен (`seed-parameter-dictionary-complete.ts`)
- [ ] Нормализация запущена (`normalize-parameters.ts`)
- [ ] Неразрешённые проанализированы (`analyze-unresolved-parameters.ts`)
- [ ] Алиасы/параметры добавлены (если нужно)
- [ ] Повторная нормализация (если были изменения)
- [ ] Индексы созданы (`009_create_normalized_indexes.sql`)
- [ ] Repository использует `normalized_parameters` ✅ (уже сделано)
- [ ] Поиск протестирован

---

## 🎉 Готово!

**Система нормализации настроена и готова к использованию!**

Вопросы? Смотри подробную документацию в `docs/27_NORMALIZATION_DETAILED_GUIDE.md`

