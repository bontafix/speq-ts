# Процесс сбора и анализа параметров

## 📋 Обзор

В проекте есть **два подхода** к созданию справочника параметров:

1. **Ручной подход** - параметры жестко зашиты в `seed-parameter-dictionary-complete.ts`
2. **Автоматический подход** - параметры генерируются через LLM из анализа БД

---

## 🔄 Полный процесс сбора и анализа

### Шаг 1: Сбор параметров из БД

**Скрипт:** `analyze-parameters.ts`

**Что делает:**
1. Извлекает **все уникальные ключи** из `equipment.main_parameters`
2. Собирает статистику по каждому параметру:
   - Частота использования (сколько раз встречается)
   - Все уникальные значения
   - Типы значений (number/string/boolean)
   - Единицы измерения (кг, т, кВт, м и т.д.)
   - Кандидаты на enum (если значений мало)

**SQL запрос:**
```sql
WITH param_keys AS (
  SELECT DISTINCT jsonb_object_keys(main_parameters) AS key
  FROM equipment
  WHERE is_active = true
    AND main_parameters IS NOT NULL
    AND main_parameters != '{}'::jsonb
),
param_stats AS (
  SELECT 
    pk.key,
    COUNT(*) as frequency,
    jsonb_agg(DISTINCT e.main_parameters->>pk.key) as all_values
  FROM param_keys pk
  CROSS JOIN equipment e
  WHERE e.main_parameters ? pk.key
    AND e.is_active = true
  GROUP BY pk.key
)
SELECT key, frequency, all_values
FROM param_stats
ORDER BY frequency DESC;
```

**Результат:** Сохраняется в `parameter-analysis.json`

**Пример результата:**
```json
{
  "key": "Мощность двигателя",
  "frequency": 133,
  "sampleValues": ["132 л.с.", "280 л.с.", "330 л.с."],
  "valueTypes": { "number": 10, "string": 0, "boolean": 0 },
  "unitPatterns": ["л.с."],
  "uniqueValuesCount": 10
}
```

### Шаг 2: Анализ параметров

**Что анализируется:**

1. **Типы значений:**
   - Числовые (132, 25.5, "132 л.с.")
   - Строковые ("Дизель", "Гусеничный")
   - Boolean (true/false, "да"/"нет")

2. **Единицы измерения:**
   - Автоматическое определение: кг, т, кВт, л.с., м, мм и т.д.

3. **Кандидаты на enum:**
   - Если уникальных значений <= 20 и они не числовые
   - Например: "Дизель", "Бензин", "Электрический"

4. **Частота использования:**
   - Сколько раз параметр встречается в БД
   - Помогает определить приоритет

---

## ❓ Почему параметры жестко зашиты в `seed-parameter-dictionary-complete.ts`?

### Причины жесткого кодирования:

#### 1. **Контроль качества**

Ручное добавление параметров позволяет:
- ✅ Тщательно проверить каждый параметр
- ✅ Убедиться в правильности типов, единиц измерения
- ✅ Добавить все необходимые алиасы
- ✅ Установить правильные приоритеты для UI

**Пример:**
```typescript
{
  key: "engine_power_kw",
  label_ru: "Мощность двигателя",
  param_type: "number",
  unit: "kw",  // Правильная единица
  aliases: [
    "Мощность двигателя",
    "мощность",
    "power",
    "Мощность, кВт",
    // ... много вариантов
  ],
  priority: 10  // Правильный приоритет для фильтров
}
```

#### 2. **Версионирование в Git**

- ✅ Все изменения отслеживаются в Git
- ✅ Можно откатить изменения
- ✅ Видна история изменений
- ✅ Легко делать code review

#### 3. **Предсказуемость**

- ✅ Всегда одинаковый результат
- ✅ Не зависит от доступности LLM
- ✅ Не зависит от качества ответов LLM
- ✅ Можно запускать без интернета

#### 4. **Производительность**

- ✅ Нет задержек на запросы к LLM
- ✅ Мгновенное выполнение
- ✅ Не тратит токены/деньги на LLM

#### 5. **Полный контроль над алиасами**

Ручное добавление позволяет:
- ✅ Добавить все варианты написания (с пробелами, без, с точками)
- ✅ Учесть опечатки в БД ("Грузоподемность" вместо "Грузоподъемность")
- ✅ Добавить английские варианты
- ✅ Добавить сокращения

**Пример с опечатками:**
```typescript
aliases: [
  "Грузоподъемность",  // Правильное написание
  "Грузоподемность",   // Опечатка в БД
  "грузоподъемность",  // С маленькой буквы
  "lifting_capacity",  // Английский вариант
]
```

---

## 🤖 Альтернативный подход: Автоматическая генерация через LLM

**Скрипт:** `generate-dictionary.ts`

### Как работает:

1. **Читает `parameter-analysis.json`** (созданный `analyze-parameters.ts`)
2. **Для каждого параметра отправляет запрос в LLM:**
   - Передает название параметра
   - Передает примеры значений
   - Передает частоту использования
   - Просит создать структуру параметра

3. **LLM генерирует:**
   - Канонический ключ (snake_case)
   - Название на русском
   - Тип параметра
   - Единицы измерения
   - Алиасы
   - Приоритет

4. **Сохраняет в БД напрямую**

### Преимущества автоматического подхода:

- ✅ Быстро создает много параметров
- ✅ Не требует ручного редактирования
- ✅ Обрабатывает все параметры из БД

### Недостатки автоматического подхода:

- ❌ Требует доступный LLM (Groq/Ollama)
- ❌ Может генерировать неточные данные
- ❌ Требует ручной проверки и корректировки
- ❌ Не версионируется в Git (сохраняется только в БД)
- ❌ Медленнее (15+ секунд на параметр)
- ❌ Тратит токены/деньги

---

## 🔄 Типичный workflow

### Вариант 1: Ручной подход (рекомендуется для критичных параметров)

```bash
# 1. Анализ параметров из БД
npx tsx src/scripts/analyze-parameters.ts
# → Создает parameter-analysis.json

# 2. Просмотр результатов
cat parameter-analysis.json | jq '.[] | select(.frequency > 10)'

# 3. Ручное добавление в seed-parameter-dictionary-complete.ts
# - Открыть файл
# - Добавить новые параметры в массив parameters
# - Добавить алиасы

# 4. Заполнить справочник
npx tsx src/scripts/seed-parameter-dictionary-complete.ts
# → Проверяет покрытие автоматически

# 5. Проверить непокрытые параметры
npx tsx src/scripts/analyze-unresolved-parameters.ts
```

### Вариант 2: Автоматический подход (для массового создания)

```bash
# 1. Анализ параметров из БД
npx tsx src/scripts/analyze-parameters.ts

# 2. Генерация через LLM (только новых, пропускает существующие)
MAX_PARAMS_TO_GENERATE=50 npx tsx src/scripts/generate-dictionary.ts

# 3. Проверка результата
npx tsx src/scripts/analyze-unresolved-parameters.ts

# 4. При необходимости - ручная корректировка в БД
# или экспорт из БД в seed-parameter-dictionary-complete.ts
```

### Вариант 3: Комбинированный (оптимальный)

```bash
# 1. Анализ
npx tsx src/scripts/analyze-parameters.ts

# 2. Автоматическая генерация для массовых параметров
MAX_PARAMS_TO_GENERATE=100 npx tsx src/scripts/generate-dictionary.ts

# 3. Проверка покрытия
npx tsx src/scripts/seed-parameter-dictionary-complete.ts

# 4. Ручное добавление критичных параметров
# - Отредактировать seed-parameter-dictionary-complete.ts
# - Добавить важные параметры вручную

# 5. Перезаполнить справочник
npx tsx src/scripts/seed-parameter-dictionary-complete.ts
```

---

## 📊 Сравнение подходов

| Критерий | Ручной подход | Автоматический подход |
|----------|---------------|----------------------|
| **Скорость** | ⚡ Мгновенно | 🐌 15+ сек на параметр |
| **Качество** | ✅ Высокое (ручная проверка) | ⚠️ Зависит от LLM |
| **Контроль** | ✅ Полный | ⚠️ Ограниченный |
| **Версионирование** | ✅ В Git | ❌ Только в БД |
| **Зависимости** | ✅ Нет | ❌ Требует LLM |
| **Стоимость** | ✅ Бесплатно | 💰 Тратит токены |
| **Масштабируемость** | ⚠️ Трудоемко | ✅ Легко масштабируется |
| **Алиасы** | ✅ Все варианты | ⚠️ Может пропустить |

---

## 🎯 Рекомендации

### Когда использовать ручной подход:

1. **Критичные параметры** (мощность, вес, грузоподъемность)
   - Нужна точность
   - Нужны все алиасы
   - Нужны правильные единицы измерения

2. **Часто используемые параметры**
   - Влияют на качество поиска
   - Нужны правильные приоритеты для UI

3. **Параметры с множеством вариантов написания**
   - Нужно учесть все опечатки
   - Нужны английские варианты

### Когда использовать автоматический подход:

1. **Редкие параметры** (встречаются 1-3 раза)
   - Не критичны для поиска
   - Можно быстро создать через LLM

2. **Массовое создание** (50+ параметров)
   - Экономит время
   - Можно потом проверить и скорректировать

3. **Экспериментальные параметры**
   - Хотите быстро протестировать
   - Потом добавите вручную если понравится

---

## 🔍 Проверка покрытия

После создания справочника (любым способом) запускается автоматическая проверка:

```typescript
async function checkCoverage() {
  // 1. Загружает справочник
  // 2. Получает все параметры из БД
  // 3. Проверяет покрытие
  // 4. Выводит отчет:
  //    - Какие параметры покрыты
  //    - Какие не покрыты (с частотой)
  //    - Категоризация (метаданные/технические/неизвестные)
  //    - Рекомендации
}
```

**Запускается автоматически** после заполнения справочника в `seed-parameter-dictionary-complete.ts`.

---

## 📝 Итог

**Параметры жестко зашиты в `seed-parameter-dictionary-complete.ts` потому что:**

1. ✅ **Контроль качества** - можно тщательно проверить каждый параметр
2. ✅ **Версионирование** - все изменения в Git
3. ✅ **Предсказуемость** - всегда одинаковый результат
4. ✅ **Производительность** - мгновенное выполнение
5. ✅ **Полный контроль** - все алиасы, единицы, приоритеты

**Но есть альтернатива:**
- Автоматическая генерация через LLM для массового создания
- Комбинированный подход для оптимального результата

**Процесс:**
1. Сбор параметров из БД → `analyze-parameters.ts`
2. Анализ → `parameter-analysis.json`
3. Создание справочника → ручной или автоматический
4. Проверка покрытия → автоматически в `seed-parameter-dictionary-complete.ts`
