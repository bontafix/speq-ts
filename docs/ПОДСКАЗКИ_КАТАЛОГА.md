# Решение: Подсказки о содержимом каталога

## Проблема

**Было:**
```
Пользователь: "какие есть трактора"
Результат: ❌ Ничего не найдено
```

Пользователь не знает:
- Есть ли вообще тракторы?
- Что доступно в каталоге?
- Может, он ошибся в названии?

## Решение

**Стало:**
```
Пользователь: "какие есть трактора"
Результат: ❌ Ничего не найдено

💡 Категория "Трактор" не найдена. Возможно, вы искали: Тягачи, Самосвалы?

📋 Что есть в каталоге:

   Популярные категории (топ-10):
   1. Краны (34 шт.)
   2. Тягачи (34 шт.)
   3. Самосвалы (32 шт.)
   4. Гусеничные краны (26 шт.)
   5. Шасси (21 шт.)
   ...
   
   Примеры запросов:
   • Покажи краны
   • Найди экскаваторы с мощностью больше 100 л.с.
   • Какие есть погрузчики марки Caterpillar
```

## Как это работает

### 1. Fallback стратегии

При нулевых результатах система пробует:

```
1. category="Трактор" → 0 результатов
   ↓
2. Fallback: text="Трактор" (полнотекстовый поиск)
   ↓
3. Если не помогло → показываем подсказки
```

### 2. Умные подсказки

**CatalogIndexService** предоставляет:

- ✅ **Похожие категории** - через fuzzy matching (расстояние Левенштейна)
- ✅ **Популярные категории** - топ по количеству оборудования
- ✅ **Примеры запросов** - как правильно искать
- ✅ **Проверка существования** - categoryExists()

### 3. Автоматическое обновление

Индекс каталога обновляется каждые 5 минут:
- Список категорий
- Список брендов
- Список регионов
- Общее количество

## Что реализовано

### ✅ SearchEngine.handleNoResults()

Fallback логика при нулевых результатах:

```typescript
// src/search/search.engine.ts строки 157-223

if (result.total === 0) {
  return await this.handleNoResults(query, limit);
}

// Fallback 1: category → text
// Fallback 2: Похожие категории
// Fallback 3: Популярные категории + примеры
```

### ✅ CatalogIndexService

Индекс каталога с методами:

```typescript
// src/catalog/catalog-index.service.ts

catalogIndex.findSimilarCategories('трактор', 5)  // ["Тягачи", "Самосвалы", ...]
catalogIndex.getPopularCategories(10)              // [{name, count}, ...]
catalogIndex.categoryExists('Краны')               // true
catalogIndex.getCategoriesForPrompt(30)            // "1. Краны (34)\n2. Тягачи (34)..."
```

### ✅ CLI отображение

Улучшенный вывод с подсказками:

```typescript
// src/cli/chat.controller.ts строки 85-120

if (result.total === 0) {
  console.log(`\n❌ Ничего не найдено`);
  
  if (result.message) {
    console.log(`\n💡 ${result.message}`);
  }
  
  if (result.suggestions) {
    this.displaySuggestions(result.suggestions);
  }
}
```

### ✅ Типы данных

```typescript
// src/catalog/catalog.types.ts

interface CatalogSearchResult {
  items: EquipmentSummary[];
  total: number;
  usedStrategy: "fts" | "vector" | "mixed" | "fallback" | "none";
  suggestions?: CatalogSuggestions;  // ← Подсказки
  message?: string;                   // ← Сообщение пользователю
}

interface CatalogSuggestions {
  similarCategories?: string[];           // Похожие категории
  popularCategories?: CategoryInfo[];     // Популярные категории
  availableBrands?: string[];             // Доступные бренды
  exampleQueries?: string[];              // Примеры запросов
}
```

## Примеры использования

### Пример 1: Несуществующая категория

**Запрос:**
```typescript
{ category: "Трактор" }
```

**Результат:**
```
❌ Ничего не найдено

💡 Категория "Трактор" не найдена. Возможно, вы искали: Тягачи, Самосвалы?

📋 Что есть в каталоге:
   Популярные категории (топ-10):
   1. Краны (34 шт.)
   2. Тягачи (34 шт.)
   3. Самосвалы (32 шт.)
   ...
```

### Пример 2: Опечатка в категории

**Запрос:**
```typescript
{ category: "кран" }  // Правильно: "Краны"
```

**Результат (fallback сработал!):**
```
✅ Найдено: 10 (Стратегия: fallback)
💡 Категория "кран" не найдена точно. Показаны результаты текстового поиска.

1. LIEBHERR LTM 1100-5.2
   Категория: Краны
   Бренд: LIEBHERR
...
```

### Пример 3: Текстовый поиск

**Запрос:**
```typescript
{ text: "бетон" }
```

**Результат:**
```
✅ Найдено: 14 (Стратегия: fts)

1. Бетонный завод LIEBHERR
2. Автобетононасос SCHWING
3. Бетоносмеситель ZOOMLION
...
```

## Тестирование

### Запуск демо-скрипта

```bash
# Демонстрация работы подсказок
npx tsx src/scripts/test-catalog-suggestions.ts
```

**Что покажет:**
- ✅ Загрузку индекса каталога
- ✅ 4 тестовых сценария с подсказками
- ✅ Примеры использования методов CatalogIndexService

### Запуск CLI чата

```bash
npm run cli
```

**Попробуйте:**
```
> какие есть трактора
❌ Ничего не найдено + подсказки

> покажи краны
✅ Найдено: 34

> что-то с бетоном
✅ Найдено: 14
```

## API методы

### SearchEngine

```typescript
const result = await searchEngine.search(query);

// result содержит:
// - items: EquipmentSummary[]
// - total: number
// - usedStrategy: string
// - suggestions?: CatalogSuggestions  ← Подсказки
// - message?: string                  ← Сообщение
```

### CatalogIndexService

```typescript
const catalogIndex = searchEngine.getCatalogIndex();

// Похожие категории
const similar = catalogIndex.findSimilarCategories('трактор', 5);
// → ["Тягачи", "Самосвалы", ...]

// Популярные категории
const popular = catalogIndex.getPopularCategories(10);
// → [{name: "Краны", count: 34}, ...]

// Проверка существования
const exists = catalogIndex.categoryExists('Краны');
// → true

// Для промпта LLM
const prompt = catalogIndex.getCategoriesForPrompt(30);
// → "1. Краны (34)\n2. Тягачи (34)\n..."
```

## Преимущества

### ✅ Для пользователя

- Всегда получает полезную информацию
- Видит, что доступно в каталоге
- Получает примеры правильных запросов
- Fallback увеличивает шанс найти нужное

### ✅ Для системы

- Автоматическое обновление индекса
- Кэширование статистики
- Нечеткий поиск (fuzzy matching)
- Graceful degradation (если что-то не работает - не ломается)

### ✅ Для разработчика

- Чистая архитектура
- Легко расширять
- Хорошее логирование
- Типобезопасность (TypeScript)

## Дальнейшие улучшения

### 1. Метрики

Отслеживать:
- Сколько раз сработал fallback
- Какие категории ищут, но не находят
- CTR на подсказки

### 2. Персонализация

Показывать подсказки на основе:
- История запросов пользователя
- Популярное в последнее время
- Сезонные тренды

### 3. Интеграция с LLM

Использовать индекс для:
- Улучшения промптов LLM
- Валидации запросов от LLM
- Автокоррекции категорий

## Файлы

**Код:**
- `src/search/search.engine.ts` - SearchEngine с fallback
- `src/catalog/catalog-index.service.ts` - CatalogIndexService
- `src/catalog/catalog.types.ts` - Типы данных
- `src/cli/chat.controller.ts` - CLI отображение

**Документация:**
- `docs/23_CATALOG_SUGGESTIONS_SOLUTION.md` - Детальное описание решения
- `docs/21_SEARCH_ISSUE_ANALYSIS.md` - Анализ проблемы

**Тесты:**
- `src/scripts/test-catalog-suggestions.ts` - Демо-скрипт

## Результат

**До:**
```
> какие есть трактора
❌ Ничего не найдено
```

**После:**
```
> какие есть трактора
❌ Ничего не найдено

💡 Категория "Трактор" не найдена. 
    Популярные категории: Краны (34), Тягачи (34), Самосвалы (32)

📋 Что есть в каталоге:
   [список категорий]
   [примеры запросов]
```

✅ **Пользователь знает, что делать дальше!**

