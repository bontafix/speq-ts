# Анализ скриптов работы с параметрами

## 📋 Обзор

Проверка всех скриптов в `src/scripts/`, которые работают с параметрами оборудования (анализ, нормализация, создание словаря).

---

## ✅ Скрипты, которые обрабатывают ВСЕ записи

### 1. `analyze-parameters.ts`
**Назначение:** Анализирует все параметры из `main_parameters` в таблице `equipment`

**Обработка записей:**
- ✅ **Обрабатывает ВСЕ активные записи**
- SQL: `WHERE is_active = true AND main_parameters IS NOT NULL AND main_parameters != '{}'::jsonb`
- Без `LIMIT` - обрабатывает все записи
- Извлекает все уникальные ключи параметров через `jsonb_object_keys(main_parameters)`

**Что делает:**
- Собирает статистику по частоте использования параметров
- Анализирует типы значений (number/string/boolean)
- Определяет единицы измерения
- Находит кандидатов на enum
- Сохраняет результаты в `parameter-analysis.json`

---

### 2. `analyze-unresolved-parameters.ts`
**Назначение:** Находит параметры из `main_parameters`, которые не были нормализованы

**Обработка записей:**
- ✅ **Обрабатывает ВСЕ активные записи**
- SQL: `WHERE is_active = true AND main_parameters IS NOT NULL AND main_parameters != '{}'::jsonb`
- Без `LIMIT` - обрабатывает все записи
- Загружает справочник и проверяет каждый параметр

**Что делает:**
- Проверяет покрытие справочника
- Показывает неразрешенные параметры с частотой использования
- Категоризирует параметры (метаданные/технические/неизвестные)
- Дает рекомендации по улучшению справочника

---

### 3. `normalize-parameters.ts`
**Назначение:** Нормализует параметры из `main_parameters` в `normalized_parameters`

**Обработка записей:**
- ⚠️ **Может обрабатывать только часть записей**
- SQL: `WHERE is_active = true AND main_parameters IS NOT NULL AND main_parameters != '{}'::jsonb`
- **ПРОБЛЕМА:** Есть опциональный параметр `limit` через `NORMALIZE_BATCH_SIZE`
- По умолчанию (без `NORMALIZE_BATCH_SIZE`) - обрабатывает все записи
- Если установлен `NORMALIZE_BATCH_SIZE` - обрабатывает только указанное количество

**Режимы работы:**
- `missing` (по умолчанию): только записи без `normalized_parameters`
- `merge`: пересчитывает и мерджит в существующий `normalized_parameters`
- `force`: пересчитывает и перезаписывает `normalized_parameters`

**Рекомендация:**
- Для полной обработки всех записей запускать без `NORMALIZE_BATCH_SIZE` или с большим значением
- После обработки проверять оставшиеся записи через `analyze-unresolved-parameters.ts`

---

## ❌ Скрипты, которые НЕ обрабатывают все записи

### 4. `seed-parameter-dictionary-complete.ts`
**Назначение:** Заполняет справочник `parameter_dictionary` статическими данными

**Обработка записей:**
- ❌ **НЕ обрабатывает записи из `equipment`**
- Не читает данные из таблицы `equipment`
- Не проверяет, какие параметры реально используются
- Только заполняет справочник статическим массивом `parameters` (~30-40 параметров)

**Проблема:**
- В БД используется **204 уникальных параметра** из `main_parameters`
- Скрипт покрывает только **~15-20%** реально используемых параметров
- Не проверяет покрытие справочника

**Рекомендация:**
- Добавить проверку всех параметров из `equipment` после заполнения справочника
- Вывести отчет о непокрытых параметрах

---

### 5. `seed-parameter-dictionary.ts`
**Назначение:** Базовая версия заполнения справочника (меньше параметров)

**Обработка записей:**
- ❌ **НЕ обрабатывает записи из `equipment`**
- Аналогично `seed-parameter-dictionary-complete.ts`
- Еще меньше параметров в статическом массиве

---

### 6. `generate-dictionary.ts`
**Назначение:** Генерирует справочник через LLM на основе анализа

**Обработка записей:**
- ⚠️ **Не работает напрямую с `equipment`**
- Использует результаты из `parameter-analysis.json` (созданного `analyze-parameters.ts`)
- Обрабатывает только топ-N параметров (по умолчанию 60)
- Фильтрует по минимальной частоте (по умолчанию 2)

**Ограничения:**
- `MAX_PARAMS_TO_GENERATE` (по умолчанию 60) - ограничивает количество
- `MIN_PARAM_FREQUENCY` (по умолчанию 2) - пропускает редкие параметры

---

### 7. `check-equipment-params.ts`
**Назначение:** Проверка параметров конкретных записей (отладочный скрипт)

**Обработка записей:**
- ❌ **Только 5 записей по имени** (`LIMIT 5`)
- Фильтрует по конкретным моделям: `SY65W`, `SY215W`, `SY135C`
- Используется для отладки, не для полного анализа

---

### 8. `show-normalized-data.ts`
**Назначение:** Показывает примеры нормализованных данных

**Обработка записей:**
- ❌ **Только примеры** (`LIMIT 2-5`)
- Показывает по 2 записи из каждой категории
- Используется для визуальной проверки, не для анализа

---

## 📊 Статистика из БД

- **Всего активных записей в `equipment`:** 844
- **Уникальных параметров в `main_parameters`:** 204
- **Параметров в `seed-parameter-dictionary-complete.ts`:** ~30-40
- **Покрытие справочника:** ~15-20%

---

## 🔧 Рекомендации

### Для полного анализа всех параметров:

1. **Запустить `analyze-parameters.ts`** - создаст `parameter-analysis.json` со всеми параметрами
2. **Запустить `analyze-unresolved-parameters.ts`** - покажет непокрытые параметры
3. **Дополнить `seed-parameter-dictionary-complete.ts`** недостающими параметрами
4. **Или использовать `generate-dictionary.ts`** для автоматической генерации через LLM

### Для нормализации всех записей:

1. **Запустить `normalize-parameters.ts` БЕЗ `NORMALIZE_BATCH_SIZE`** - обработает все записи
2. **Или запускать с `NORMALIZE_BATCH_SIZE=1000`** и повторять до обработки всех
3. **Проверить результат через `analyze-unresolved-parameters.ts`**

### Улучшения для `seed-parameter-dictionary-complete.ts`:

1. Добавить проверку покрытия после заполнения справочника
2. Вывести отчет о непокрытых параметрах
3. Показать статистику: сколько параметров покрыто, сколько нет

---

## 📝 Выводы

### ✅ Хорошо работают:
- `analyze-parameters.ts` - обрабатывает все записи
- `analyze-unresolved-parameters.ts` - обрабатывает все записи
- `normalize-parameters.ts` - обрабатывает все записи (если без limit)

### ⚠️ Требуют внимания:
- `normalize-parameters.ts` - может ограничиваться `NORMALIZE_BATCH_SIZE`
- `generate-dictionary.ts` - обрабатывает только топ-N параметров

### ❌ Проблемы:
- `seed-parameter-dictionary-complete.ts` - не проверяет реальные данные из БД
- `seed-parameter-dictionary.ts` - не проверяет реальные данные из БД
- Оба скрипта заполняют справочник только статическими данными

---

## 🎯 Приоритетные задачи

1. **Добавить проверку покрытия в `seed-parameter-dictionary-complete.ts`**
   - После заполнения справочника проверить все параметры из `equipment`
   - Вывести отчет о непокрытых параметрах

2. **Улучшить `normalize-parameters.ts`**
   - Добавить автоматическую обработку всех записей по батчам
   - Показывать прогресс обработки

3. **Создать скрипт для полной проверки покрытия**
   - Сравнивает все параметры из `equipment` со справочником
   - Генерирует отчет с рекомендациями
