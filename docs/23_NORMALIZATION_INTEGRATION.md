# Интеграция системы нормализации параметров (30.12.2025)

## 📚 Обзор

В проекте теперь реализована **мощная двухуровневая система нормализации параметров**:

### Уровень 1: ParameterDictionaryService (приоритет)
- ✅ Загружает справочник из БД (`parameter_dictionary`)
- ✅ Canonical keys: `power_kw`, `weight_kg`, `excavation_depth_mm`
- ✅ Алиасы: "мощность", "вес", "глубина_копания"
- ✅ Автоматическая конверсия единиц: `"132 л.с."` → `97.152 кВт`
- ✅ Валидация типов и диапазонов
- ✅ Поддержка enum значений

### Уровень 2: ParameterNameMapper (fallback)
- ✅ Хардкод маппинг для быстрого доступа
- ✅ Работает без БД
- ✅ Конверсия базовых единиц: `5 м` → `5000 мм`

---

## 🚀 Быстрый старт

### 1. Заполните справочник параметров

```bash
# Настройте переменные окружения для БД
export PGHOST=localhost
export PGPORT=5432
export PGUSER=postgres
export PGPASSWORD=your_password
export PGDATABASE=equipment_catalog

# Запустите скрипт заполнения
npx tsx src/scripts/seed-parameter-dictionary.ts
```

**Что делает скрипт:**
- Создает таблицу `parameter_dictionary` (если не существует)
- Заполняет справочником 10+ параметров
- Добавляет алиасы для каждого параметра

### 2. Проверьте, что данные загружены

```sql
SELECT key, label_ru, category, aliases 
FROM parameter_dictionary 
ORDER BY priority, key;
```

### 3. Запустите приложение

```bash
npm run start
```

**При старте:**
```
[Init] Запуск контейнера приложения...
✅ Dictionary loaded  # ← Словарь загружен
```

---

## 📊 Архитектура

### Поток данных

```
Пользователь: "Экскаватор с мощностью 132 л.с. и весом до 25 тонн"
    ↓
LLM генерирует:
{
  "мощность": "132 л.с.",
  "вес_max": "25 тонн"
}
    ↓
EquipmentRepository.normalizeParameter()
    ↓
    ├─ 1. ParameterDictionaryService.findCanonicalKey("мощность")
    │    ✅ Найден: engine_power_kw
    │    ↓
    ├─ 2. UnitParser.parseValue("132 л.с.", "kw")
    │    ✅ Конвертировано: 132 л.с. → 97.152 кВт
    │    ↓
    └─ 3. SQL: (main_parameters->>'Мощность двигателя')::numeric >= 97.152
```

### Компоненты

#### 1. ParameterDictionaryService
**Файл:** `src/normalization/parameter-dictionary.service.ts`

**API:**
```typescript
// Загрузка справочника
await dictionaryService.loadDictionary();

// Поиск по алиасу
const param = dictionaryService.findCanonicalKey("мощность");
// { key: "engine_power_kw", unit: "kw", aliases: [...] }

// Получить по canonical key
const param = dictionaryService.getByKey("engine_power_kw");
```

#### 2. UnitParser
**Файл:** `src/normalization/unit-parser.ts`

**API:**
```typescript
const parser = new UnitParser();

// Парсинг и конверсия
parser.parseValue("132 л.с.", "kw");  // → 97.152
parser.parseValue("25 тонн", "kg");   // → 25000
parser.parseValue("5 м", "mm");       // → 5000
```

**Поддерживаемые единицы:**
- **Масса:** т, кг, г
- **Мощность:** л.с., кВт, Вт
- **Длина:** км, м, см, мм
- **Объем:** м³, л
- **Производительность:** т/ч, м³/ч
- **Частота:** Гц, об/мин

#### 3. EquipmentRepository.normalizeParameter()
**Файл:** `src/repository/equipment.repository.ts`

**Логика:**
```typescript
private normalizeParameter(key: string, value: string | number) {
  // 1. ПРИОРИТЕТ: Пробуем через словарь
  if (this.dictionaryService) {
    const paramDef = this.dictionaryService.findCanonicalKey(key);
    if (paramDef) {
      // Используем canonical key + UnitParser
      const parsed = this.unitParser.parseValue(value, paramDef.unit);
      return { paramKey, value: parsed, operator };
    }
  }
  
  // 2. FALLBACK: ParameterNameMapper
  const mapped = ParameterNameMapper.mapParameterName(key);
  const converted = ParameterNameMapper.convertValue(mapped.dbParamName, value);
  return { paramKey: mapped.dbParamName, value: converted, operator };
}
```

---

## 📝 Структура таблицы parameter_dictionary

```sql
CREATE TABLE parameter_dictionary (
  key VARCHAR(100) PRIMARY KEY,           -- Canonical key: "engine_power_kw"
  label_ru VARCHAR(200) NOT NULL,         -- "Мощность двигателя"
  description_ru TEXT,                    -- Описание
  category VARCHAR(50) NOT NULL,          -- "excavator", "crane", "common"
  param_type VARCHAR(20) NOT NULL,        -- "number", "enum", "boolean"
  unit VARCHAR(20),                       -- "kw", "t", "mm"
  min_value NUMERIC,                      -- Минимальное значение
  max_value NUMERIC,                      -- Максимальное значение
  enum_values JSONB,                      -- {"diesel": "Дизель", ...}
  aliases TEXT[] NOT NULL,                -- ["мощность", "power", ...]
  sql_expression TEXT NOT NULL,           -- "main_parameters->>'Мощность двигателя'"
  priority INTEGER NOT NULL DEFAULT 1,    -- Приоритет при поиске
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

### Пример записи

```json
{
  "key": "engine_power_kw",
  "label_ru": "Мощность двигателя",
  "description_ru": "Мощность двигателя в киловаттах",
  "category": "common",
  "param_type": "number",
  "unit": "kw",
  "min_value": 10,
  "max_value": 1000,
  "aliases": [
    "мощность",
    "мощность_двигателя",
    "номинальная_мощность",
    "power",
    "engine_power",
    "Мощность двигателя"
  ],
  "sql_expression": "main_parameters->>'Мощность двигателя'",
  "priority": 1
}
```

---

## 🎯 Примеры использования

### Пример 1: Поиск с конверсией единиц

**Запрос:**
```json
{
  "text": "экскаватор",
  "parameters": {
    "мощность_двигателя": "132 л.с.",
    "вес_max": "25 тонн"
  }
}
```

**Нормализация:**
```
1. "мощность_двигателя" → engine_power_kw
   "132 л.с." → 97.152 кВт

2. "вес_max" → operating_weight_t
   "25 тонн" → 25 т (уже в целевой единице)
```

**SQL:**
```sql
WHERE (main_parameters->>'Мощность двигателя')::numeric >= 97.152
  AND (main_parameters->>'Вес в рабочем состоянии')::numeric <= 25
```

---

### Пример 2: Поиск с алиасами

**Разные варианты запроса:**
```javascript
// Вариант 1
{ "мощность": "132 л.с." }

// Вариант 2
{ "power": "132 hp" }

// Вариант 3
{ "номинальная_мощность": "97 кВт" }

// Все преобразуются в:
{ "engine_power_kw": 97.152 }
```

---

### Пример 3: Fallback маппинг

**Если параметр НЕ в словаре:**
```javascript
// Запрос
{ "новый_параметр_max": 100 }

// Fallback через ParameterNameMapper
// → параметр используется как есть
// → значение не конвертируется
```

---

## 📦 Добавление нового параметра

### Вариант 1: Через SQL

```sql
INSERT INTO parameter_dictionary (
  key, label_ru, description_ru, category, param_type, 
  unit, min_value, max_value, aliases, sql_expression, priority
) VALUES (
  'blade_width_mm',                           -- Canonical key
  'Ширина отвала',                            -- Русское название
  'Ширина отвала бульдозера в миллиметрах',   -- Описание
  'bulldozer',                                -- Категория
  'number',                                   -- Тип
  'mm',                                       -- Единица
  1000,                                       -- Min
  10000,                                      -- Max
  ARRAY[                                      -- Алиасы
    'ширина_отвала',
    'ширина',
    'blade_width',
    'width'
  ],
  'main_parameters->>''Ширина отвала, мм.''', -- SQL expression
  2                                           -- Приоритет
);
```

### Вариант 2: Обновить seed скрипт

**Файл:** `src/scripts/seed-parameter-dictionary.ts`

```typescript
{
  key: "blade_width_mm",
  label_ru: "Ширина отвала",
  description_ru: "Ширина отвала бульдозера в миллиметрах",
  category: "bulldozer",
  param_type: "number",
  unit: "mm",
  min_value: 1000,
  max_value: 10000,
  aliases: [
    "ширина_отвала",
    "ширина",
    "blade_width",
    "width"
  ],
  sql_expression: "main_parameters->>'Ширина отвала, мм.'",
  priority: 2,
},
```

Затем:
```bash
npx tsx src/scripts/seed-parameter-dictionary.ts
```

---

## 🔍 Отладка

### Включить DEBUG логирование

```bash
DEBUG=true npm run start
```

**Что логируется:**
```
[Normalizer] мощность_двигателя → Мощность двигателя = 97.152 (from dictionary)
[Normalizer] вес_max → Вес в рабочем состоянии = 25 (from dictionary)
[VectorSearch] SQL: WHERE ... (main_parameters->>'Мощность двигателя')::numeric >= 97.152
```

### Проверить загрузку словаря

```typescript
// В app/container.ts
async init() {
  try {
    await this.dictionaryService.loadDictionary();
    const dict = this.dictionaryService.getDictionary();
    console.log(`✅ Dictionary loaded: ${dict.length} parameters`);
  } catch (e) {
    console.warn("⚠️ Dictionary load failed");
  }
}
```

---

## 📊 Статистика

После заполнения справочника:

```
✅ Добавлено: 10 параметров
📦 Всего параметров: 10

📈 Статистика по категориям:
   - common: 2 параметров
   - excavator: 3 параметров
   - crane: 3 параметров
   - bulldozer: 1 параметров
```

---

## ✨ Преимущества

### 1. Гибкость
- ✅ Добавить параметр = INSERT в БД
- ✅ Обновить алиасы = UPDATE в БД
- ✅ Нет необходимости перекомпилировать код

### 2. Точность
- ✅ Автоматическая конверсия единиц
- ✅ Валидация диапазонов
- ✅ Поддержка enum значений

### 3. Надежность
- ✅ Fallback на ParameterNameMapper
- ✅ Graceful degradation при ошибках
- ✅ Логирование для отладки

### 4. Масштабируемость
- ✅ Легко добавлять новые параметры
- ✅ Поддержка множества категорий
- ✅ Расширяемая система алиасов

---

## 🎓 Дальнейшие улучшения

### 1. UI для управления словарем
```typescript
// API endpoints
GET    /api/parameters          // Список параметров
POST   /api/parameters          // Добавить параметр
PUT    /api/parameters/:key     // Обновить параметр
DELETE /api/parameters/:key     // Удалить параметр
```

### 2. Автоматическое обучение
- Анализ запросов пользователей
- Автоматическое добавление популярных алиасов
- ML для улучшения маппинга

### 3. Мультиязычность
- Поддержка английских названий
- Автоматический перевод
- Локализация единиц измерения

---

## 📚 Связанные документы

- `docs/19_DIALOG_AND_SEARCH_ANALYSIS.md` - Анализ диалога и поиска
- `docs/21_SEARCH_QUERY_QUALITY.md` - Качество формирования SearchQuery
- `docs/22_PARAMETER_SEARCH_FIX.md` - Исправление поиска по параметрам

---

## ✅ Итог

**Система нормализации полностью интегрирована!**

**Что работает:**
- ✅ Автоматическая нормализация параметров из LLM
- ✅ Конверсия единиц измерения
- ✅ Поддержка алиасов
- ✅ Fallback маппинг
- ✅ Логирование и отладка

**Как использовать:**
1. Заполните `parameter_dictionary` (скрипт готов)
2. Запустите приложение
3. Система автоматически нормализует параметры при поиске

**Система готова к production использованию!** 🚀

