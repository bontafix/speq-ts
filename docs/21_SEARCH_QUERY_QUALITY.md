# Улучшение качества формирования SearchQuery (30.12.2025)

## 🎯 Проблема

Пользователь заметил, что в консоли выводился JSON **не полностью**:
- ❌ Показывались только `parameters`  
- ❌ НЕ показывались: `text`, `category`, `subcategory`, `brand`, `region`
- ❌ Промпты LLM были недостаточно детальны
- ❌ Не было примеров хороших SearchQuery
- ❌ Отсутствовала валидация ответов от LLM

## ✅ Решение

### 1. Полное логирование SearchQuery

**Было** (`chat.controller.ts`):
```typescript
console.log(`\n🔍 Ищу в базе данных... (${step.query.text || "по параметрам"})`);

if (step.query.parameters && Object.keys(step.query.parameters).length > 0) {
   console.log("   Параметры:", JSON.stringify(step.query.parameters));
}
```

**Стало**:
```typescript
console.log(`\n🔍 Ищу в базе данных...`);
console.log("\n📋 Сформированный запрос SearchQuery:");
console.log(JSON.stringify(step.query, null, 2));
console.log("");
```

**Результат**: Теперь в консоли показываются **ВСЕ поля** SearchQuery:
```json
{
  "text": "экскаватор гусеничный",
  "category": "Экскаватор",
  "brand": "Caterpillar",
  "region": "Москва",
  "parameters": {
    "объем_ковша_min": 1,
    "грузоподъемность_min": 80
  },
  "limit": 10
}
```

---

### 2. Улучшенные промпты с примерами

#### Было (InteractiveQueryBuilder):
```
Правила:
- Не придумывай параметры...
- "text" — краткая суть запроса (2-10 слов).
- "parameters" используй для тех. характеристик...
```

#### Стало (с детальными объяснениями):
```
ВАЖНО! Разница между полями:
- "text" — используется для ВЕКТОРНОГО (семантического) поиска.
  Пример: "экскаватор для земляных работ", "гусеничный кран"
  
- "category", "subcategory", "brand", "region" — для ТОЧНОЙ фильтрации в БД.
  Примеры категорий: "Экскаватор", "Кран", "Погрузчик"
  Примеры брендов: "Caterpillar", "Komatsu", "Hitachi"
  
- "parameters" — технические характеристики (JSONB поле).
  Используй РУССКИЕ названия: "грузоподъемность", "мощность", "вес"

Правила formирования parameters:
- Для "более/больше/от X" → "_min": {"грузоподъемность_min": 80}
- Для "менее/меньше/до X" → "_max": {"тоннаж_max": 25}
- Для точного значения: {"мощность": 150}
```

#### Добавлены примеры:
```
Примеры хороших SearchQuery:

Запрос: "Нужен экскаватор Caterpillar с ковшом от 1 кубометра"
Ответ: {
  "action":"final",
  "query":{
    "text":"экскаватор",
    "category":"Экскаватор",
    "brand":"Caterpillar",
    "parameters":{"объем_ковша_min":1}
  }
}

Запрос: "Покажи краны грузоподъемностью более 80 тонн в Москве"
Ответ: {
  "action":"final",
  "query":{
    "text":"кран",
    "category":"Кран",
    "region":"Москва",
    "parameters":{"грузоподъемность_min":80}
  }
}
```

**Результат**: LLM теперь понимает:
- ✅ Разницу между векторным поиском (`text`) и точной фильтрацией (`category`, `brand`)
- ✅ Когда использовать суффиксы `_min`/`_max`
- ✅ Что использовать русские названия параметров
- ✅ Видит конкретные примеры правильных запросов

---

### 3. Валидация SearchQuery от LLM

Создан новый класс `SearchQueryValidator` (`src/llm/search-query.validator.ts`):

#### Что проверяется:

**Типы данных:**
```typescript
// text, category, brand, region - должны быть строками
if (typeof query.text === "string") {
  validated.text = query.text.trim();
}

// limit - число от 1 до 100
if (typeof query.limit === "number") {
  validated.limit = Math.min(Math.max(query.limit, 1), 100);
} else if (typeof query.limit === "string") {
  // Автоматическое преобразование "15" → 15
  validated.limit = parseInt(query.limit, 10);
}
```

**Безопасность параметров:**
```typescript
// Защита от SQL инъекций через имена параметров
private static isValidParameterKey(key: string): boolean {
  return /^[a-zA-Zа-яА-ЯёЁ0-9_]+$/.test(key) && key.length < 100;
}

if (!this.isValidParameterKey(key)) {
  issues.push(`Некорректное имя параметра: "${key}" (пропущено)`);
  continue; // Пропускаем опасный параметр
}
```

**Значения параметров:**
```typescript
// Проверка на NaN, Infinity
if (typeof value === "number") {
  if (Number.isFinite(value)) {
    validated.parameters[key] = value;
  } else {
    issues.push(`Параметр "${key}" имеет некорректное числовое значение`);
  }
}
```

**Длины строк:**
```typescript
// text - максимум 500 символов
if (trimmed.length > 500) {
  validated.text = trimmed.substring(0, 500);
  issues.push(`text обрезан до 500 символов`);
}

// category, brand, region - максимум 100 символов
// parameters values - максимум 200 символов
```

#### Результаты валидации (примеры из тестов):

✅ **Позитивные случаи:**
- Полный запрос с всеми полями - проходит
- Автоматическое преобразование `limit: "15"` → `limit: 15`
- Нормализация `limit: 500` → `limit: 100` (ограничение)
- Обрезка пробелов: `"  кран  "` → `"кран"`

✅ **Блокировка опасных данных:**
```
Параметр: "'; DROP TABLE equipment; --" - ОТКЛОНЕН
NaN и Infinity значения - ОТКЛОНЕНЫ
Слишком длинные строки - ОБРЕЗАНЫ
Пустые строки - ПРОИГНОРИРОВАНЫ
```

---

### 4. Интеграция валидатора

**InteractiveQueryBuilder:**
```typescript
import { SearchQueryValidator } from "./search-query.validator";

function parseStepJson(raw: string): InteractiveQueryStep {
  // ... парсинг JSON ...
  
  if (action === "final") {
    const query = parsed?.query;
    
    // Валидируем и нормализуем SearchQuery от LLM
    try {
      const validatedQuery = SearchQueryValidator.validate(query);
      return { action: "final", query: validatedQuery };
    } catch (error: any) {
      throw new Error(`Некорректный SearchQuery от LLM: ${error.message}`);
    }
  }
}
```

**QuestionParser:**
```typescript
async parse(userText: string): Promise<SearchQuery> {
  // ... получение ответа от LLM ...
  
  // Валидируем и нормализуем SearchQuery
  try {
    return SearchQueryValidator.validate(parsed);
  } catch (error: any) {
    throw new Error(`Некорректный SearchQuery от LLM: ${error.message}`);
  }
}
```

---

## 📊 Тестирование

Создан комплексный тест: `src/scripts/test-search-query-quality.ts`

**16 тестовых сценариев:**
```
✅ Полный запрос с всеми полями
✅ Только text и параметры
✅ Только category и brand
✅ Limit как строка (нормализация)
✅ Limit за пределами (нормализация 1-100)
✅ Пробелы в строках (обрезка)
✅ Параметры с разными типами
✅ Некорректное имя параметра (SQL injection попытка)
✅ Некорректный тип для text (не строка)
✅ Некорректный тип для limit (строка с буквами)
✅ Параметры с некорректными значениями
✅ Пустые строки (должны игнорироваться)
✅ Слишком длинные строки (обрезка)
✅ Пустой объект (корректно выбросила ошибку)
✅ Null вместо объекта (корректно выбросила ошибку)
✅ parameters не объект (массив)
```

**Результат:** 16/16 PASS (100%)

Запуск тестов:
```bash
npx tsx src/scripts/test-search-query-quality.ts
```

---

## 📁 Измененные файлы

| Файл | Изменения | Строк |
|------|-----------|-------|
| `src/cli/chat.controller.ts` | Полное логирование SearchQuery | +3/-4 |
| `src/llm/interactive-query.builder.ts` | Улучшенный промпт + валидация | +60/-20 |
| `src/llm/question.parser.ts` | Улучшенный промпт + валидация | +30/-10 |
| `src/llm/search-query.validator.ts` | Новый файл: валидатор | +180 |
| `src/scripts/test-search-query-quality.ts` | Новый файл: тесты | +380 |

**Итого:** ~650 строк кода и комментариев

---

## 🎯 Достигнутые улучшения

### 1. Прозрачность 🔍
**Было:** Выводились только `parameters`
**Стало:** Выводятся **все поля** SearchQuery с форматированием

### 2. Качество промптов 📝
**Было:** 
- Короткие инструкции
- Без примеров
- Неясная разница между полями

**Стало:**
- Детальные объяснения каждого поля
- 4 примера хороших запросов
- Чёткая разница: векторный поиск vs точная фильтрация

### 3. Безопасность 🔐
**Было:** Данные от LLM принимались как есть
**Стало:**
- ✅ Валидация всех полей
- ✅ Защита от SQL инъекций в именах параметров
- ✅ Фильтрация NaN, Infinity
- ✅ Ограничение длин строк
- ✅ Нормализация типов данных

### 4. Надёжность 🛡️
**Было:** LLM мог вернуть некорректные типы → падение
**Стало:**
- ✅ Автоматическая нормализация
- ✅ Логирование проблем
- ✅ Graceful degradation (некорректные поля просто пропускаются)

---

## 📈 Пример работы (до и после)

### ДО исправлений:
```
Пользователь: "Экскаватор Caterpillar с ковшом от 1 куба в Москве"

🔍 Ищу в базе данных... (экскаватор)
   Параметры: {"объем_ковша_min":1}

❌ Проблемы:
- Не видно category: "Экскаватор"
- Не видно brand: "Caterpillar"
- Не видно region: "Москва"
- Не видно text для векторного поиска
```

### ПОСЛЕ исправлений:
```
Пользователь: "Экскаватор Caterpillar с ковшом от 1 куба в Москве"

🔍 Ищу в базе данных...

📋 Сформированный запрос SearchQuery:
{
  "text": "экскаватор",
  "category": "Экскаватор",
  "brand": "Caterpillar",
  "region": "Москва",
  "parameters": {
    "объем_ковша_min": 1
  },
  "limit": 10
}

✅ Найдено: 5 (Стратегия: mixed)

✅ Преимущества:
- Видны ВСЕ параметры запроса
- text используется для векторного поиска
- category, brand, region - для точной фильтрации
- parameters валидированы и безопасны
```

---

## 🚀 Влияние на качество поиска

### Векторный поиск (pgvector)
**Используется поле `text`:**
- ✅ Теперь LLM понимает, что сюда нужно краткое описание
- ✅ Примеры: "экскаватор гусеничный", "кран башенный"
- ✅ Улучшает семантический поиск

### Точная фильтрация (FTS + SQL WHERE)
**Используются поля `category`, `brand`, `region`, `parameters`:**
- ✅ LLM извлекает точные значения
- ✅ Категория и бренд пишутся с большой буквы (как в БД)
- ✅ Параметры с суффиксами `_min`/`_max` работают корректно

### Hybrid Search (RRF)
**Объединение FTS + Vector:**
- ✅ Оба режима получают корректные данные
- ✅ Результаты ранжируются по RRF
- ✅ Итоговая выдача более релевантна

---

## 💡 Рекомендации по использованию

### Для разработчиков:

1. **Смотрите логи SearchQuery** - они теперь полные и читаемые
2. **Следите за warnings валидатора** - они показывают проблемы с данными от LLM
3. **При изменении промптов** - добавляйте примеры в формате `Запрос: → Ответ:`

### Для пользователей (через CLI):

1. **Будьте конкретны** - указывайте категорию, бренд, параметры явно
2. **Используйте диапазоны** - "от 80 тонн", "до 20 кубов", "более 150 л.с."
3. **Указывайте регион** - если важно: "в Москве", "в Санкт-Петербурге"

### Примеры хороших запросов:

✅ "Экскаватор Caterpillar с объемом ковша от 1 кубометра"
✅ "Кран грузоподъемностью более 80 тонн в Москве"
✅ "Гусеничный бульдозер весом до 20 тонн"
✅ "Погрузчик фронтальный Komatsu с мощностью от 150 л.с."

---

## ✨ Итоги

**Проблема решена:**
- ✅ Полное логирование SearchQuery
- ✅ Улучшенные промпты с примерами
- ✅ Валидация и защита от некорректных данных
- ✅ 16 автоматических тестов (100% pass)

**Качество парсинга значительно улучшено!** 🎉

