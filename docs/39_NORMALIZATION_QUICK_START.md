# Быстрый старт: Нормализация параметров

## 📋 Очередность команд

### 1. Анализ существующих параметров

Анализирует все параметры из `main_parameters` и создает файл `parameter-analysis.json`:

```bash
npm run analyze:parameters
# или
npx tsx src/scripts/analyze-parameters.ts
```

**Результат:**
- Файл `parameter-analysis.json` с анализом всех параметров
- Вывод в консоль топ-30 наиболее частых параметров

---

### 2. Создание/заполнение справочника

#### Вариант А: Готовый справочник (рекомендуется для начала)

Заполняет справочник готовыми параметрами с алиасами:

```bash
npx tsx src/scripts/seed-parameter-dictionary-complete.ts
```

**Что делает:**
- Создает таблицу `parameter_dictionary` (если не существует)
- Заполняет 16+ готовых параметров с алиасами
- Готов к использованию сразу

#### Вариант Б: Генерация через LLM (для новых параметров)

Генерирует справочник на основе анализа параметров:

```bash
npm run generate:dictionary
# или
npx tsx src/scripts/generate-dictionary.ts
```

**Что делает:**
- Читает `parameter-analysis.json` (созданный на шаге 1)
- Генерирует записи справочника через LLM для топ-50 параметров
- Сохраняет в таблицу `parameter_dictionary`

**Настройки (через переменные окружения):**
```bash
MIN_PARAM_FREQUENCY=3              # минимальная частота использования
MAX_PARAMS_TO_GENERATE=50          # максимальное количество параметров
LLM_MODEL=qwen2.5:7b-instruct-q4_K_M  # модель LLM
```

---

### 3. Нормализация параметров

Преобразует `main_parameters` → `normalized_parameters`:

```bash
npm run normalize:parameters
# или
npx tsx src/scripts/normalize-parameters.ts
```

**Режимы работы:**

```bash
# По умолчанию: только записи без normalized_parameters
NORMALIZE_MODE=missing npx tsx src/scripts/normalize-parameters.ts

# Объединить с существующими (добавить новые параметры)
NORMALIZE_MODE=merge npx tsx src/scripts/normalize-parameters.ts

# Полный пересчет (перезаписать все)
NORMALIZE_MODE=force npx tsx src/scripts/normalize-parameters.ts
```

**Обработка батчами:**

```bash
# Обработать только 100 записей за раз
NORMALIZE_BATCH_SIZE=100 npx tsx src/scripts/normalize-parameters.ts
```

---

### 4. Анализ неразрешенных параметров

Анализирует параметры, которые не удалось нормализовать:

```bash
npx tsx src/scripts/analyze-unresolved-parameters.ts
```

**Что показывает:**
- Список неразрешенных параметров
- Частота их использования
- Примеры значений
- Рекомендации по добавлению в справочник

---

## 🔄 Типичный workflow

### Первый запуск (полный цикл):

```bash
# 1. Анализ
npm run analyze:parameters

# 2. Заполнение справочника
npx tsx src/scripts/seed-parameter-dictionary-complete.ts

# 3. Нормализация
npm run normalize:parameters

# 4. Проверка результатов
npx tsx src/scripts/analyze-unresolved-parameters.ts
```

### Обновление справочника:

```bash
# 1. Анализ неразрешенных
npx tsx src/scripts/analyze-unresolved-parameters.ts

# 2. Редактирование справочника (добавить алиасы/параметры)
# Отредактировать: src/scripts/seed-parameter-dictionary-complete.ts

# 3. Перезаполнить справочник
npx tsx src/scripts/seed-parameter-dictionary-complete.ts

# 4. Пересчитать нормализацию
NORMALIZE_MODE=merge npm run normalize:parameters

# 5. Проверить результат
npx tsx src/scripts/analyze-unresolved-parameters.ts
```

### Нормализация новых записей:

```bash
# Обработать только новые записи (без normalized_parameters)
npm run normalize:parameters
```

---

## ⚙️ Переменные окружения

```bash
# Режим нормализации (missing/merge/force)
NORMALIZE_MODE=missing

# Размер батча для обработки
NORMALIZE_BATCH_SIZE=100

# Отладочный режим
DEBUG=true

# LLM настройки (для генерации справочника)
LLM_MODEL=qwen2.5:7b-instruct-q4_K_M
MIN_PARAM_FREQUENCY=3
MAX_PARAMS_TO_GENERATE=50
```

---

## ✅ Проверка результатов

### SQL запросы:

```sql
-- Количество нормализованных записей
SELECT COUNT(*) 
FROM equipment 
WHERE normalized_parameters IS NOT NULL 
  AND normalized_parameters != '{}'::jsonb;

-- Процент нормализации
SELECT 
  COUNT(*) as total,
  COUNT(normalized_parameters) FILTER (
    WHERE normalized_parameters != '{}'::jsonb
  ) as normalized,
  ROUND(100.0 * COUNT(normalized_parameters) FILTER (
    WHERE normalized_parameters != '{}'::jsonb
  ) / COUNT(*), 2) as percent
FROM equipment
WHERE is_active = true;

-- Примеры нормализованных параметров
SELECT 
  name,
  main_parameters,
  normalized_parameters
FROM equipment
WHERE normalized_parameters != '{}'::jsonb
LIMIT 5;
```

---

## 🚨 Важно

1. **Справочник должен быть заполнен** перед нормализацией
   - Если справочник пуст, скрипт завершится с ошибкой
   - Запустите сначала: `npx tsx src/scripts/seed-parameter-dictionary-complete.ts`

2. **Неразрешенные параметры не теряются**
   - Они остаются в `main_parameters`
   - Просто не попадают в `normalized_parameters`
   - Можно добавить их в справочник позже

3. **Режим `force` перезаписывает данные**
   - Используйте осторожно!
   - Лучше сначала протестировать на небольшой выборке

4. **Нормализация идет по батчам**
   - Если обрабатываете большие объемы, используйте `NORMALIZE_BATCH_SIZE`
   - Запускайте скрипт несколько раз до полной обработки

---

## 📚 Дополнительная документация

- **Подробное руководство:** `docs/27_NORMALIZATION_DETAILED_GUIDE.md`
- **Шпаргалка:** `docs/NORMALIZATION_CHEATSHEET.md`
- **Объяснение скрипта:** `docs/35_NORMALIZE_PARAMETERS_EXPLAINED.md`
- **Автоматизация:** `docs/12_PARAMETER_NORMALIZATION_AUTOMATION.md`
